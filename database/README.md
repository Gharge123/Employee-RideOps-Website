# PostgreSQL database

Run `CREATE DATABASE rideops;` once, then start the backend. The API automatically executes `schema.sql` and, for an empty database, `seed.sql`.

Runtime application data is stored in PostgreSQL; there is no JSON database file.
